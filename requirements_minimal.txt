# Minimal requirements for ARM64 compatibility
# Core dependencies that work on Windows ARM64
colorama==0.4.6
requests==2.31.0
pillow==10.3.0
psutil==5.9.8
pydantic==2.11.7
pydantic_core==2.33.2
pytest==8.4.1
python-dateutil==2.9.0
PyYAML==6.0.1
tqdm==4.66.4
urllib3==1.26.20

# Optional - comment out if they fail
# numpy  # Let pip choose compatible version
# opencv-python-headless  # Headless version is more ARM64 friendly
# torch  # Will need ARM64 compatible version
# qai-hub  # Snapdragon X Elite specific
# qai-hub-models  # Snapdragon X Elite specific

# These are problematic on ARM64 - commented out
# onnxruntime  # No ARM64 wheels yet
# h5py  # Requires compilation
# gmpy2  # Requires compilation  
# matplotlib  # Requires compilation
# scipy  # Requires compilation 